// Safe client-only config stub. Do not put secrets here.
// To use direct Worker fallback in local dev, copy js/config.sample.js to js/config.js and fill values locally.
window.IMAGENESIS_CFG = window.IMAGENESIS_CFG || {};